# 🐸 Tiny Frog Transfer

Allows to upload and retrieve files

[app](https://tiny-frog.pages.dev)

# deploy

```sh


# deploy everything
( cd src ; bunx wrangler pages deploy --branch=production )

# change api key
( cd src ; bunx wrangler pages secret put STORE_API_KEY )

```
